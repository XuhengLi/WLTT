#!/bin/bash
# WLTTBackendDB
set -e 

if [ ! -f var/WLTTBackend.sqlite3 ]; then
	set -x
	./bin/WLTTBackendDB create	
fi

export FLASK_DEBUG=True
export FLASK_APP=WLTTBackend
export WLTTBACKEND_SETTINGS=config.py

flask run --host 0.0.0.0 --port 8000 